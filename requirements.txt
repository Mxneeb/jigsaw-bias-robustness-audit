# ============================================================
# requirements.txt — Assignment 2: Content Moderation AI Audit
# Responsible & Explainable AI | FAST-NUCES
# Python 3.10  |  Tested locally with CUDA 12.1 GPU
# ============================================================

# Core ML / NLP
# NOTE: huggingface-hub must stay <1.0 to be compatible with transformers
torch==2.5.1
transformers==4.57.1
huggingface-hub>=0.34.0,<1.0
accelerate>=0.30.0
tokenizers>=0.19.0

# Tabular / data
pandas==2.3.3
numpy==1.26.4

# Metrics & classic ML
scikit-learn==1.7.2

# Fairness libraries
fairlearn==0.13.0
aif360==0.6.1

# Plotting
matplotlib==3.8.4
seaborn==0.13.2

# Utilities
tqdm>=4.66.0

# Protobuf must stay <4.0 — aif360 uses generated _pb2 files that break with protobuf>=4
protobuf>=3.20.0,<4.0.0
